# 🔄 설정 메뉴 뒤로가기 버튼 해결 가이드

## 🚨 문제 상황

설정 메뉴에서 "뒤로가기" 버튼을 눌러도 반응이 없음

## ⚡ 즉시 해결 방법

### 방법 1: 자동 수정 (권장)

1. **MenuUIGenerator 사용**:
    ```
    - Hierarchy에서 빈 GameObject 생성 → "UIGenerator"
    - MenuUIGenerator.cs 스크립트 추가
    - Inspector에서 "자동 메뉴 UI 생성" 클릭
    - 자동으로 뒤로가기 버튼이 연결됨!
    ```

### 방법 2: 수동 설정

1. **GameMenuManager 설정**:

    ```
    - Hierarchy에서 GameMenuManager 오브젝트 선택
    - Inspector에서 "Back Button" 필드 찾기
    - Canvas/SettingsPanel/SettingsContent/BackButton을 드래그해서 할당
    ```

2. **뒤로가기 버튼 확인**:
    ```
    - Canvas/SettingsPanel/SettingsContent 하위에 BackButton이 있는지 확인
    - 없다면 수동으로 버튼 생성:
      * SettingsContent 우클릭 → UI → Button
      * 이름을 "BackButton"으로 변경
      * 텍스트를 "뒤로가기"로 변경
    ```

## 🔧 고급 해결 방법

### ESC 키로도 뒤로가기 (이미 구현됨)

```
설정 메뉴가 열린 상태에서:
- ESC 키 누르기 → 자동으로 설정 메뉴 닫힘
- 메인 메뉴로 돌아감
```

### 뒤로가기 버튼 이벤트 수동 연결

Unity Editor에서:

```
1. BackButton 선택
2. Inspector에서 "On Click ()" 찾기
3. "+" 버튼 클릭
4. GameMenuManager 오브젝트를 드래그
5. "No Function" → GameMenuManager → CloseSettings() 선택
```

## 🧪 테스트 방법

1. **Play 모드 진입**
2. **ESC 키 눌러서 메뉴 열기**
3. **"설정" 버튼 클릭**
4. **"뒤로가기" 버튼 클릭** → 메인 메뉴로 돌아가야 함
5. **ESC 키로도 테스트** → 설정 메뉴에서 ESC 누르면 메인 메뉴로 돌아가야 함

## 🔍 문제 진단

### Console에서 확인할 로그들:

-   `✅ 뒤로가기 버튼 이벤트 연결 완료` → 정상
-   `⚠️ 뒤로가기 버튼이 할당되지 않았습니다!` → 문제 있음

### Inspector에서 확인할 것들:

-   GameMenuManager의 "Back Button" 필드에 버튼이 할당되어 있는지
-   BackButton의 "On Click ()" 이벤트가 설정되어 있는지

## 📋 체크리스트

뒤로가기 버튼이 정상 작동하려면:

-   [ ] Canvas/SettingsPanel/SettingsContent/BackButton 존재
-   [ ] GameMenuManager의 BackButton 필드에 할당됨
-   [ ] 버튼의 On Click 이벤트가 CloseSettings()에 연결됨
-   [ ] Play 모드에서 뒤로가기 버튼 클릭 시 작동
-   [ ] ESC 키로도 설정 메뉴에서 나가짐

## 🆘 그래도 안 되면?

1. **Console 확인**: 에러 메시지나 경고가 있는지 확인
2. **EventSystem 확인**: Hierarchy에 EventSystem이 있는지 확인
3. **Button 컴포넌트 확인**: BackButton에 Button 컴포넌트가 있는지 확인
4. **UI 생성 다시**: MenuUIGenerator로 UI를 다시 생성해보기

모든 설정이 완료되면 설정 메뉴에서 뒤로가기 버튼과 ESC 키 모두 정상 작동합니다!
